# Required Python packages

runpod~=1.7.9
hf_transfer

# DeepSeek-OCR-2 dependencies (mirrors upstream requirements.txt)
# NOTE: torch==2.6.0 and torchvision==0.21.0 are installed separately in the
# Dockerfile from the cu118 index to match the CUDA 11.8 base image.
transformers==4.46.3
tokenizers>=0.20,<0.21
einops
addict
easydict
PyMuPDF
img2pdf
Pillow
numpy
